const Restart = ({playAgain}) => {
  
    return (
      // eslint-disable-next-line eqeqeq
      <div >
          <button onClick={playAgain} id="restartBtn">Restart</button> 
      </div>
    )
  }
  
  export default Restart